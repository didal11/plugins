# 5호선 열차별 혼잡도 추정

## 목표

평일·토요일·일요일 유형과 출발시간으로 종로3가 → 화곡의 시간표상 후보 열차와 정차역별 예상 혼잡도를 비교합니다. 첫 버전은 열차 전체 평균이며 객차별·실시간 혼잡도는 제공하지 않습니다.

경로: 종로3가, 광화문, 서대문, 충정로, 애오개, 공덕, 마포, 여의나루, 여의도, 신길, 영등포시장, 영등포구청, 양평, 오목교, 목동, 신정, 까치산, 화곡.

## 현재 상태

구현 완료: 프로젝트 설정, 상태·환경 확인 CLI와 BAT, 개발·평가 설계, 기존 예제 사본 보관.

구현 예정: 원본 수집 및 검증, 시간표 결합, 기준 추정, Keras 학습·평가, LiteRT 변환. 아직 혼잡도 예측이 실행되는 완성본은 아닙니다.

## 개발 순서

1. 여러 기준기간의 혼잡도 원본과 열차운행시각표 수집·검증.
2. 열차코드로 정차역 연결, 구간 운행시간과 앞 열차 간격 계산.
3. 정차시각에 대응하는 30분 평균을 조회하는 기준 추정 구현.
4. Keras로 평균 혼잡도 회귀, 별도 기간에서 기준 모델과 비교.
5. 방향별 승하차·OD 배분 근거 확보 후 열차별 차이 추정 실험.
6. 검증한 모델의 LiteRT 변환 및 TFMOT 경량화 실험.

## 원본과 출처

| 데이터 | 예정 파일명 | 용도 |
| --- | --- | --- |
| 혼잡도 | data/raw/congestion_YYYYMMDD.xlsx 또는 .csv | 역·방향·요일유형·30분 평균 혼잡도(%) |
| 시간표 | data/raw/timetable_YYYYMMDD.csv | 열차코드·역·요일유형·방향·도착/출발시각 |
| 승하차·OD | 확보 후 정의 | 수요 보조자료; 집계 단위·환승 처리 확인 후 사용 |

- 혼잡도: https://data.seoul.go.kr/dataList/OA-12928/A/1/datasetView.do
- 시간표: https://www.data.go.kr/data/15098251/fileData.do
- 시간대별 승하차 후보: https://data.seoul.go.kr/dataList/OA-12252/S/1/datasetView.do
- 역간 소요시간 후보: https://data.seoul.go.kr/dataList/OA-12034/S/1/datasetView.do

파일명은 수집 규칙이며 다운로드 완료를 의미하지 않습니다. 원본별 URL·다운로드 시각·기준기간·시간표 적용기간·SHA256·이용조건을 기록합니다. 기준일과 관측기간을 동일하게 가정하지 않습니다. 서로 다른 기간의 결합은 시나리오로 표시합니다.

## 시간표와 데이터 검증

- 공식 역코드를 우선 사용하고 역명·호선·방향을 정규화합니다.
- DAY/SAT/END를 요일유형으로 변환합니다. 일요일 혼잡도 통계의 공휴일 적용 여부는 별도 확인합니다.
- 24·25시를 영업일 시작 이후 초로 보존합니다.
- 요일유형·방향·열차코드와 필요한 기종점 식별자로 운행을 구분합니다. 키 중복을 조용히 제거하지 않습니다.
- 종로3가→화곡의 정차 순서와 증가하는 시각으로 방향을 검증합니다. UP/DOWN을 임의 배정하지 않습니다.
- 종착역 출발시각 결측은 도착시각을 사용하되 기록합니다. 중간역 시각 결측은 보고합니다.
- 주행시간 = 다음 역 도착 - 현재 역 출발. 정차시간 = 출발 - 도착.
- 배차간격 = 같은 역·방향·요일유형의 앞 열차와 출발시각 차이. 첫 열차는 결측입니다.
- 조인 실패·결측을 0으로 채우지 않습니다. 원본 0은 비운행 여부를 검토합니다. 혼잡도는 100%를 넘을 수 있으므로 100에서 자르지 않습니다.

## 가공 출력

한 행은 한 영업일 유형에서 한 열차의 한 역 정차입니다.

`schedule_version, congestion_period, day_type, train_id, direction, station_code, station_name, stop_order, arrival_seconds, departure_seconds, headway_seconds, next_run_seconds, congestion_bin, published_mean_pct, estimated_congestion_pct, estimate_method, quality_flags`

공개 평균과 열차에 할당한 추정값을 각각 보존합니다. 평균을 여러 열차 행에 붙여도 독립적인 실제 정답 수가 늘지는 않습니다. 가공 결과는 data/processed/, 모델은 models/line5/, 평가 결과는 results/line5/에 둡니다.

## 모델·Loss·통과 조건

첫 기준 추정은 역·방향·요일유형·30분 구간 평균 조회입니다. 같은 구간의 열차는 같은 평균을 받습니다.

첫 Keras 모델은 작은 Dense 회귀모델입니다. 역·요일유형 등의 범주형 정보와 시간의 sin/cos를 입력하고 혼잡도(%) 하나를 출력합니다. 정규화와 범주 사전은 훈련 데이터에서만 만듭니다.

Loss: 집계 평균에 대한 Huber loss. 평가: MAE(%p), RMSE(%p), 출퇴근 시간대 MAE. 80%를 90%로 예측하면 오차는 10%p입니다.

기준기간을 시간순으로 훈련/검증/시험에 분리합니다. 같은 평균이 복제된 열차 행을 무작위 분할하지 않습니다. 한 기간만 확보하면 기간 외 성능 평가 불가로 표시합니다. 시험기간 평균 조회는 예측 성능 비교 기준이 될 수 없습니다. 비교 기준은 훈련기간에서 산출한 같은 역·요일유형·시간대 평균입니다.

통과: 사전에 정한 별도 시험기간의 전체 MAE가 기준 모델보다 낮고 출퇴근 MAE가 악화되지 않아야 합니다. 이는 집계 평균 성능이며 실제 개별 열차 성능의 검증은 아닙니다.

## 열차별 확장 시 주의

개찰구 승차량은 특정 방향 열차의 탑승량과 같지 않습니다. 노선·방향 선택, 환승, 승차까지 지연, 상류역 초기 탑승인원, 만차 잔류 인원을 고려해야 합니다.

인원 보존식: 다음 역 출발 인원 = 현재 역 출발 인원 - 다음 역 하차 + 다음 역 승차. 승하차와 초기 인원에 대한 근거가 있을 때 적용합니다.

열차 예측을 30분 단위로 모아 공개 평균과 비교하는 약한 감독 학습은 후속 실험입니다. 공식 집계 가중치를 확인해야 하며, 동일한 평균을 만드는 열차별 배분이 여러 개라 집계 Loss만으로 실제 열차별 혼잡도를 식별할 수 없습니다. 배차간격 배율이나 생성 라벨은 가정으로 표시합니다.

## LiteRT / TFMOT

검증된 Keras 모델을 float/INT8 LiteRT로 변환합니다. INT8 대표 샘플은 훈련 데이터에서 선택합니다. 동일 시험 입력으로 변환 전후 예측 차이·MAE·파일 크기·추론시간을 비교하고 허용 오차는 시험 전에 정합니다.

TFMOT pruning/clustering은 별도 실험입니다. Keras 3와 legacy tf-keras 모델을 섞지 않고 호환성을 검증합니다. 작은 모델에서 경량화 이득이 생기는지도 실측합니다.
